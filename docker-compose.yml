version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    env_file: ./server/.env
    volumes:
      - ./server:/app
    depends_on: []

  client:
    build: ./client
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app